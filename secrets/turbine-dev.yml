---
host: 198.13.51.15

# Items that apply to ALL profiles:
eureka:
  instance:
    hostname: ${host}
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8001/eureka/

turbine:
  app-config: leaderboard
  cluster-name-expression: default
